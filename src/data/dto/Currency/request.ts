export type CurrencyRequestDTO = {
   name?: string;
   code?: string;
   codeFormat?: string;
   symbol?: string;
}

/**
 * @typedef {object} CurrencyRequestDTO
 * @property {string} name
 * @property {string} code
 * @property {string} codeFormat
 * @property {string} symbol
 */

