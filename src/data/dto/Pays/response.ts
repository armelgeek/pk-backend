export type PaysResponseDTO = {
   name?: string;
   code?: string;
   emoji?: string;
  id: string;
}

/**
 * @typedef {object} PaysResponseDTO
 * @property {string} name
 * @property {string} code
 * @property {string} emoji
 */

