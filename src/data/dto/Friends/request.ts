export type FriendsRequestDTO = {
   follow?: string;
   follower?: string;
   confirmed?: boolean;
   cancel?: boolean;
}

/**
 * @typedef {object} FriendsRequestDTO
 * @property {string} follow
 * @property {string} follower
 * @property {boolean} confirmed
 * @property {boolean} cancel
 */

