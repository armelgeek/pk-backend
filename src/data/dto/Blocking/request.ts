export type BlockingRequestDTO = {
   profileId?: string;
   blockingProfile?: string;
   reason?: string;
}

/**
 * @typedef {object} BlockingRequestDTO
 * @property {string} profileId
 * @property {string} blockingProfile
 * @property {string} reason
 */

