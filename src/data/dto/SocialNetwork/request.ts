export type SocialNetworkRequestDTO = {
   name?: string;
   base_url?: string;
   profileId?: string;
}

/**
 * @typedef {object} SocialNetworkRequestDTO
 * @property {string} name
 * @property {string} base_url
 * @property {string} profileId
 */

