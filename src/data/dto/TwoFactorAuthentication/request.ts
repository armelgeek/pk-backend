export type TwoFactorAuthenticationRequestDTO = {
   type?: string;
   actif?: boolean;
}

/**
 * @typedef {object} TwoFactorAuthenticationRequestDTO
 * @property {string} type
 * @property {boolean} actif
 */

