export type AuthentificationRequestDTO = {
   email: string;
   password: string;
}

/**
 * @typedef {object} AuthentificationRequestDTO
 * @property {string} email
 * @property {string} password
 */

