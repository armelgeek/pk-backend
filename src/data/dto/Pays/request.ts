export type PaysRequestDTO = {
   name?: string;
   code?: string;
   emoji?: string;
}

/**
 * @typedef {object} PaysRequestDTO
 * @property {string} name
 * @property {string} code
 * @property {string} emoji
 */

