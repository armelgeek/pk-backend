export type FollowersRequestDTO = {
   follow?: string;
   follower?: string;
   friends?: boolean;
   confirmed?: boolean;
   cancel?: boolean;
}

/**
 * @typedef {object} FollowersRequestDTO
 * @property {string} follow
 * @property {string} follower
 * @property {boolean} friends
 * @property {boolean} confirmed
 * @property {boolean} cancel
 */

